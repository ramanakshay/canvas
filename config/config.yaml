env:
    name: Acrobot-v1
    max_ep_steps: 500
    obs_dim: 6
    act_dim: 3

agent:
    obs_dim: ${env.obs_dim}
    act_dim: ${env.act_dim}
    hidden_dim: 64
    learning_rate: 1e-3

algorithm:
    total_timesteps: 150000
    timesteps_per_batch: 2500
    lam: 0.9
    gamma: 0.99